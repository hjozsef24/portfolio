.news {
    h4 {
        +maxwidth($md-max) {
            font-size 24px
            line-height 32px
        }
    }

    p {
        +maxwidth($md-max) {
            font-size 14px
            line-height 20px
        }
    }

    .btn {
        +maxwidth($md-max) {
            margin-top 24px
        }
    }
}