.filter--shop-the-look {
    &--extra {
        display: flex;
        justify-content: space-between;

        .reset-shop-the-look-filters {
            text-transform: uppercase;
            font-size: 16px;
            cursor: pointer;
            display: none;
        }
    }

    &--results {
        color: $info-text-color;
        font-size: 16px;
        line-height: 24px;
    }
}